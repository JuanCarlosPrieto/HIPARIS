# Backend par défaut : OWLv2 (HuggingFace transformers)
# >=4.51 requis pour post_process_grounded_object_detection (testé avec 5.8.1).
transformers>=4.51
torch>=2.0
Pillow>=9.0
scipy>=1.10

# Serveur local pour le bouton « Annotation IA » du HTML
fastapi>=0.110
uvicorn>=0.29

# Fournisseurs Vision-LLM (installe seulement ceux que tu utilises) :
# openai>=1.30          # ChatGPT (gpt-4o…)
# anthropic>=0.49       # Claude (claude-opus-4-8…)
# google-genai>=1.0     # Gemini (gemini-2.5-flash…)

# Backend alternatif (--backend yoloworld) :
# ultralytics>=8.3.0

# GPU (optionnel) : installer torch depuis https://pytorch.org selon ta version CUDA.
